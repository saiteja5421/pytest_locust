from enum import Enum


class OnboardingTemplateProperties(Enum):
    NEW_ONBOARDING_TEMPLATE_FEATURES = (
        "Account needs update to Onboarding Template. "
        "Apply new version of template, to make use of latest backup and recovery features"
    )
    LATEST_VERSION = "3.0.0"
